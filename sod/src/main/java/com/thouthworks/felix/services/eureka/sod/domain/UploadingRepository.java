package com.thouthworks.felix.services.eureka.sod.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface UploadingRepository extends JpaRepository<Uploading, Long> {
}
